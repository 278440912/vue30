import vueButtonJinshui from './components/vue-button-jinshui'

const obj = {
  install(Vue, opts = {}) {
    Vue.component(vueButtonJinshui.name, vueButtonJinshui);
  }
};

if (window.Vue) {
  window.Vue.use(obj)
}
export default obj;
